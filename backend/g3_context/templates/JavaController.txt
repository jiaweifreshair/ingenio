package com.ingenio.backend.module.${moduleName}.controller;

import com.ingenio.backend.common.api.Result;
import com.ingenio.backend.module.${moduleName}.entity.${EntityName};
import com.ingenio.backend.module.${moduleName}.service.${EntityName}Service;
import com.ingenio.backend.module.${moduleName}.dto.${EntityName}DTO;
import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.tags.Tag;
import lombok.RequiredArgsConstructor;
import org.springframework.web.bind.annotation.*;
import jakarta.validation.Valid;

import java.util.List;

/**
 * ${EntityName} 控制器
 * Generated by Ingenio G3 (Player Agent)
 */
@RestController
@RequestMapping("/api/v1/${resourceName}")
@RequiredArgsConstructor
@Tag(name = "${EntityName} Management", description = "${description}")
public class ${EntityName}Controller {

    private final ${EntityName}Service service;

    @GetMapping
    @Operation(summary = "获取列表")
    public Result<List<${EntityName}>> list() {
        return Result.success(service.list());
    }

    @GetMapping("/{id}")
    @Operation(summary = "根据ID获取")
    public Result<${EntityName}> getById(@PathVariable String id) {
        return Result.success(service.getById(id));
    }

    @PostMapping
    @Operation(summary = "创建")
    public Result<String> create(@Valid @RequestBody ${EntityName}DTO dto) {
        service.create(dto);
        return Result.success("Created successfully");
    }

    @PutMapping("/{id}")
    @Operation(summary = "更新")
    public Result<String> update(@PathVariable String id, @Valid @RequestBody ${EntityName}DTO dto) {
        service.update(id, dto);
        return Result.success("Updated successfully");
    }

    @DeleteMapping("/{id}")
    @Operation(summary = "删除")
    public Result<String> delete(@PathVariable String id) {
        service.removeById(id);
        return Result.success("Deleted successfully");
    }
}
