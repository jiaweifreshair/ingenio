# Ingenio Smart Builder Phase 1 - 综合实施主计划 (Master Plan)

**版本**: v2.0 (Comprehensive Fusion)
**日期**: 2025-12-25
**状态**: **APPROVED FOR EXECUTION**
**整合来源**:
1.  `02-PHASE1_SMART_BUILDER_UI_ENHANCEMENT_PLAN.md` (UI/UX Design)
2.  `08-G3_ENGINE_IMPLEMENTATION_PLAN.md` (G3 Red-Blue Engine)
3.  `10-PHASE1_INTEGRATED_MASTER_PLAN.md` (Execution Roadmap)

---

## 目录

1.  [核心愿景: 模版增强生成 (TAG)](#1-核心愿景-模版增强生成-tag)
2.  [G3 引擎内核: 红蓝博弈](#2-g3-引擎内核-红蓝博弈)
3.  [系统架构与数据流](#3-系统架构与数据流)
4.  [资产模型与 G3 上下文](#4-资产模型与-g3-上下文)
5.  [用户旅程与 UI 设计](#5-用户旅程与-ui-设计)
6.  [前端改造方案](#6-前端改造方案)
7.  [实施路线图](#7-实施路线图)
8.  [验收标准](#8-验收标准)

---

## 1. 核心愿景: 模版增强生成 (TAG)

我们将从简单的“代码生成”升级为 **Template-Augmented Generation (TAG)** 模式。系统不再试图从零幻觉出一个复杂的企业级应用，而是基于经过验证的“骨架”进行“灵魂注入”。

### 1.1 双流融合架构

| 维度 | Java 侧 (JeecgBoot) | Python 侧 (AgentScope) |
| :--- | :--- | :--- |
| **隐喻** | **骨架 (Skeleton)** | **灵魂 (Soul)** |
| **生成方式** | **填空式 (Deterministic)**<br>基于 Controller/Service 标准模版生成 | **创造式 (Creative)**<br>基于 AgentScope 框架编写业务逻辑 |
| **核心职责** | 鉴权、持久化、事务管理、调用第三方能力 (支付/用户) | 推理、多轮对话、工具调用、知识检索、意图路由 |
| **连接方式** | 自动生成 `AgentClient` (HTTP RestTemplate) | 自动生成 `server.py` (FastAPI/Flask) |

### 1.2 价值主张
*   **稳定性**: 核心业务（CRUD、鉴权）由 Java 模版保证，绝对稳健。
*   **智能化**: 复杂逻辑（如“智能审批”、“语义搜索”）由 Python Agent 实现，灵活多变。
*   **可交付**: 交付的是一套完整的、可编译运行的“Java + Python”双栈工程。

---

## 2. G3 引擎内核: 红蓝博弈

G3 (Guidance, Generation, Guardrails) 引擎不再是一个简单的重试循环，而是一个**“虚拟研发团队”**的攻防博弈场。

### 2.1 角色定义

#### 🟦 蓝方 (Player Agent) - "全栈工程师"
*   **职责**：读取需求和模版，编写 Java 和 Python 代码。
*   **目标**：实现功能，通过编译，通过测试。
*   **工具**：`backend/g3_context/` (能力注册表, 代码模版)。
*   **Prompt 特征**: "你是构建者，请基于模版实现功能..."

#### 🟥 红方 (Coach Agent) - "QA / 黑客"
*   **职责**：**不写业务代码，只写测试代码**。
*   **策略**：
    *   **边界攻击**：生成空值、超长文本、非法字符。
    *   **逻辑攻击**：模拟 API 超时、数据库断连。
    *   **业务攻击**：模拟未授权访问、积分不足调用。
*   **产出**：`test_attack.py` (针对 Agent) 或 `ServiceTest.java` (针对 Java)。
*   **Prompt 特征**: "你是攻击者，请编写能把这段代码跑崩的测试用例..."

#### ⚖️ 裁判 (Executor) - "物理引擎"
*   **职责**：在沙箱中运行测试。
*   **判定**：
    *   测试失败 -> ❌ **红方胜** -> 报错日志甩给蓝方 -> **进入下一轮 Loop**。
    *   测试通过 -> ✅ **蓝方胜** -> **代码交付**。

---

## 3. 系统架构与数据流

### 3.1 智能流式管道 (Smart Streaming Pipeline)

为了解决“后台博弈耗时久，前台无���馈”的问题，API 响应协议升级为 **"Log + Code"** 双流模式：

```
Server (SSE) --------------------------------------> Client
[Thinking Phase]
LOG: [Coach] 正在生成 SQL 注入攻击测试...
LOG: [Executor] ❌ 测试失败，InventoryService 未校验负库存。
LOG: [Player] 正在修复库存校验逻辑...
LOG: [Executor] ✅ 所有测试通过。

[Delivering Phase]
CODE: <file path="src/...">...</file>
DONE
```

### 3.2 架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           前端 (Next.js 15)                              │
│  • 模板长廊 (Select) -> 能力配置 (Config) -> 触发生成 (Generate)         │
│  • 实时展示 G3 博弈日志 (Log Stream)                                     │
└───────────┬─────────────────────────────────────────────────────────────┘
            │ POST /api/generate-agent-code
            ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                           BFF / Lab (Next.js)                           │
│  • G3 Engine Orchestrator (调度器)                                       │
│  • Prompt Factory (组装 Player/Coach 提示词)                             │
└───────────┬─────────────────────────┬───────────────────────────────────┘
            │ 1. Read Context         │ 2. Loop Execution
            ▼                         ▼
┌───────────────────────┐ ┌───────────────────────────────────────────────┐
│   Backend Context     │ │                 E2B / Local Sandbox           │
│  (backend/g3_context) │ │                                               │
│  • Capabilities.md    │ │  🟦 Player: 生成代码 (Java/Python)             │
│  • Templates/*.txt    │ │  🟥 Coach:  生成测试 (Pytest/JUnit)            │
│  • Jeecg Configs      │ │  ��️ Executor: 运行测试 & 返回 Logs              │
└───────────────────────┘ └───────────────────────────────────────────────┘
```

---

## 4. 资产模型与 G3 上下文

### 4.1 G3 上下文 (`backend/g3_context`)

这是 G3 的“知识库”，决定了它能调用哪些 JeecgBoot 原生能力。

| 文件名 | 用途 | 内容示例 |
| :--- | :--- | :--- |
| `capabilities.md` | 能力注册表 | 定义 `IUserService.deductPoints()`, `IAgentService.chat()` 等接口签名 |
| `templates/JavaController.txt` | Java 骨架 | Spring Boot `@RestController` 标准结构，含 `Result<T>` 封装 |
| `templates/JavaService.txt` | Java 逻辑 | 含 `@Autowired` 注入点和 Agent 调用示例的 Service 结构 |
| `templates/AgentScope.txt` | Python 骨架 | AgentScope `main.py` 和 `agent.py` 的标准结构 |

### 4.2 资产数据模型 (Frontend Types)

```typescript
// types/template.ts
export interface Template {
  id: string;
  name: string; // e.g., "电商客服助手"
  sceneCategory: SceneCategory;
  capabilities: Capability[]; // e.g., ["NLU", "RAG", "PAYMENT"]
}

export interface Capability {
  id: string;
  capabilityType: 'JAVA_SERVICE' | 'PYTHON_AGENT';
  apiSignature?: string; // 对于 Java 能力，记录接口签名
  promptPack?: PromptPack; // 对于 Agent 能力，记录提示词模板
}
```

---

## 5. 用户旅程与 UI 设计

### 5.1 完整流程

1.  **模板选择 (Selection)**:
    *   用户浏览“行业模板长廊”（从 JeecgBoot API 获取或降级本地）。
    *   选择 “VIP 专属导购” 模板。
2.  **能力配置 (Configuration)**:
    *   勾选需要的 Java 能力：✅ 用户中心 (积分扣除) ✅ 支付中心 (订单创建)。
    *   勾选需要的 Python 能力：✅ 导购 Agent (多轮对话)。
3.  **G3 生成 (Generation)**:
    *   用户点击“生成全栈应用”。
    *   **UI 变化**: 进入“构建中”状态，左侧显示文件树占位符，右侧显示 **"G3 博弈日志"** (Terminal 风格)。
4.  **交付与预览 (Delivery)**:
    *   生成完成，自动下载/展示代码。
    *   提供“一键部署”或“启动预览”按钮。

---

## 6. 前端改造方案

### 6.1 核心组件

#### 6.1.1 `G3LogViewer.tsx` (新组件)
用于展示红蓝博弈的实时日志。

```typescript
// components/generation/G3LogViewer.tsx
export default function G3LogViewer({ logs }: { logs: G3LogEntry[] }) {
  return (
    <div className="bg-gray-900 text-green-400 font-mono p-4 rounded-lg h-96 overflow-y-auto">
      {logs.map((log, i) => (
        <div key={i} className="mb-2">
          <span className="opacity-50">[{log.timestamp}]</span>
          <span className={
            log.role === 'COACH' ? 'text-red-400 font-bold' : 
            log.role === 'PLAYER' ? 'text-blue-400 font-bold' : 'text-yellow-400'
          }> [{log.role}] </span>
          {log.message}
        </div>
      ))}
    </div>
  );
}
```

#### 6.1.2 首页与生成页改造
*   `app/page.tsx`: 集成 `TemplateCard` 和 `CapabilitySelector`。
*   `app/preview-quick/[requirement]/page.tsx`: 集成 `G3LogViewer` 和流式触发逻辑。

### 6.2 API 路由改造

*   **`app/api/generate-agent-code/route.ts`**:
    *   接收 `templateId`, `capabilities`, `requirement`。
    *   初始化 `G3Engine`。
    *   返回 `ReadableStream`，推送日志和最终代码。

---

## 7. 实施路线图

### 阶段一：基础建设 (Foundation)
*目标：建立 G3 上下文和数据模型。*
*   [ ] 1.1 创建 `backend/g3_context/` 及核心描述文件 (`capabilities.md`, templates)。
*   [ ] 1.2 定义前端 TypeScript 类型 (`types/template.ts`)。
*   [ ] 1.3 创建 `lib/assets/adapter.ts` (资产适配器)。

### 阶段二：内核实现 (Kernel)
*目标：跑通红蓝博弈循环。*
*   [ ] 2.1 实现 `lib/lab/g3-engine.ts` (核心调度器)。
*   [ ] 2.2 编写 `lib/g3/prompts.ts` (Player/Coach 提示词工程)。
*   [ ] 2.3 实现 Mock Executor (暂用内存检查代替真实沙箱)。

### 阶段三：UI 集成 (Integration)
*目标：用户可见、可交互。*
*   [ ] 3.1 改造首页 `app/page.tsx`。
*   [ ] 3.2 开发 `G3LogViewer` 组件。
*   [ ] 3.3 联调流式 API。

---

## 8. 验收标准

1.  **架构合规**: 生成的代码必须包含 Java Controller/Service 和 Python Agent 目录，且 Java 代码中包含调用 Python 服务的逻辑。
2.  **博弈真实**: 在生成日志中，必须能看到 Coach 提出质疑（或测试失败）并触发 Player 修复的过程。
3.  **能力复用**: 生成的 Java 代码必须正确引用 `capabilities.md` 中定义的 JeecgBoot 接口（如 `IUserService`），而不是瞎编接口。
4.  **可编译**: 最终交付的代码（Java + Python）在理论上应能通过语法检查。