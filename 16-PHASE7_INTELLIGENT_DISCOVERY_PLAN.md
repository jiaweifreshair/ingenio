# Phase 7: Intelligent Discovery (智能选型与探索) 实施方案

**版本**: v1.0
**日期**: 2025-12-30
**状态**: 核心已就绪，正在进行前端集成

---

## 1. 核心目标

Phase 7 旨在将 Ingenio 从“代码生成器”升级为“解决方案发现引擎”。
在真正开始写代码之前���系统会先派出 **Repo Scout Agent (代码侦察兵)** 去 GitHub 寻找最佳的参考实现。

**流程变革**:
*   **Before**: 用户需求 -> AI 凭空生成代码 (容易产生幻觉，甚至编译错误)。
*   **After**: 用户需求 -> Scout 搜索并下载成熟模版 -> AI 基于模版修改 (极高成功率)。

---

## 2. 架构设计

### 2.1 后端架构 (G3 Engine)
*   **服务地址**: `http://localhost:8000`
*   **核心组件**:
    *   `RepoScoutAgent`: 模拟 GitHub Search，智能分析仓库匹配度。
    *   `TemplateManager`: 负责 Git Clone 和 Sparse Checkout。
    *   `Registry`: `backend/g3_context/template-registry.json` 定义受信任的源。
*   **API**:
    *   `POST /api/v1/g3/scout`: 启动侦察任务。
    *   `GET /api/v1/g3/logs/{task_id}`: 获取实时侦察日志。

### 2.2 前端集成 (Next.js)
*   **代理层**: Next.js API Route (`/api/g3/scout`) 转发请求到 Python 引擎，避免 CORS 问题。
*   **交互层**: `QuickPreviewPage` 新增 `scouting` 阶段。
*   **UI 组件**: `ScoutTerminal` 展示侦察兵的思考过程和搜索结果。

---

## 3. 执行步骤

1.  **Python 引擎**: 已实现 `RepoScoutAgent` 和 `TemplateManager`。
2.  **API 代理**: 在 Next.js 中建立通往 Python 的桥梁。
3.  **UI 改造**: 修改预览页面，���入侦察环节。

---

## 4. 预期效果

用户输入 "Jeecg Stripe Payment" 后：
1.  UI 显示 `🕵️ Scout Agent is searching GitHub...`
2.  日志流滚动显示:
    *   `Found candidate: jeecg-boot-module-demo (Score: 85)`
    *   `Found candidate: stripe-java (Score: 40)`
3.  Scout 推荐最佳模版。
4.  系统自动下载该模版并开始基于此生成代码。

---
**Ingenio Architecture Team**
