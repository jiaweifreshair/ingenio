# 测试环境变量配置
# 秒构AI前端测试环境 - Week 1 Day 3 Phase 3.1

# Next.js配置
NODE_ENV=test
NEXT_TELEMETRY_DISABLED=1

# API地址配置 - 连接真实后端开发环境（遵循零Mock策略）
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
NEXT_PUBLIC_AGENT_SERVICE_URL=http://localhost:8000

# 后端Java服务地址（用于Next.js API路由代理到后端）
# 注意：后端配置了 context-path=/api，因此需要包含 /api 前缀
BACKEND_API_URL=http://localhost:8080/api

# 应用配置
NEXT_PUBLIC_APP_NAME=灵构Creator
NEXT_PUBLIC_APP_VERSION=1.0.0

# 调试模式（测试环境开启）
NEXT_PUBLIC_DEBUG=true

# 测试配置
# Vitest单元测试超时时间（毫秒）
VITEST_TIMEOUT=10000

# Playwright E2E测试超时时间（毫秒）
PLAYWRIGHT_TIMEOUT=30000

# 数据库配置（如果测试需要直接访问数据库）
# 注意：正式环境应使用TestContainers启动真实PostgreSQL
TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ingenio_test

# Redis配置（如果测试需要）
TEST_REDIS_URL=redis://localhost:6379/1

# 安全配置
# 测试环境JWT密钥（仅用于测试，不要在生产使用）
TEST_JWT_SECRET=test-jwt-secret-key-for-testing-only-do-not-use-in-production
